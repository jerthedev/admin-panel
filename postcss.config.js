/**
 * PostCSS Configuration for Admin Panel - Tailwind v4 Compatible
 *
 * Tailwind CSS v4 uses the @tailwindcss/vite plugin instead of PostCSS.
 * This file is kept for compatibility but is not used in the build process.
 *
 * @author Jeremy Fall <jerthedev@gmail.com>
 * @version 1.0.0 - Tailwind v4 Compatible
 */

export default {
    plugins: {
        // Tailwind v4 processing is handled by @tailwindcss/vite plugin
        // autoprefixer is included automatically
    },
}
